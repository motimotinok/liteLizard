# LiteLizard タスク管理

> **目標**: GitHub Pages に MVP を公開する（`/liteLizard/` で動くデモ）
> **最終更新**: 2026-03-05

---

## 🖥️ ダッシュボード

> **このセクションには常に「今すぐやるべき1タスク」だけを表示する。**
> タスクが完了したら、Claudeが次の最優先タスクに更新する。

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[T1] ブラウザ起動時にモックAPIを自動注入する
ファイル : apps/desktop/src/renderer/main.tsx
内容     : window.litelizard 未定義時に createMockPreloadApi() をセット
工数     : 30分
完了条件 : npm run dev 後にブラウザで白画面にならずアプリが起動する
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 📋 実行タスク（優先度順）

### ルール（Claude用）
- タスクは優先度の高い順に並べる
- 優先度判断はClaudeが開発者視点で客観的に行う
- ユーザーが「これを先にやって欲しい」と明示した場合はそれを最優先にする
- 新規タスクはWORKSPACEから抽出後、優先度を判断して適切な位置に挿入する

---

### [T1] ブラウザ起動時にモックAPIを自動注入する
- **ファイル**: `apps/desktop/src/renderer/main.tsx`
- **内容**: `window.litelizard` が未定義のとき `createMockPreloadApi()` をセット
- **工数**: 30分
- **完了条件**: `npm run dev` 後にブラウザで白画面にならずアプリが起動する
- **状態**: ⬜ 未着手

### [T2] AnalysisPane に「生成」ボタンを追加する
- **ファイル**: `apps/desktop/src/renderer/components/AnalysisPane.tsx`
- **内容**: ヘッダーに「生成」ボタン → `runAnalysis()` 呼び出し。stale=0件 or pending中は disabled
- **工数**: 1時間
- **完了条件**: ボタンを押すと段落カードに解析結果（emotion/theme）が表示される
- **状態**: ⬜ 未着手

> T1・T2 が完了するとブラウザでアプリが起動・解析デモができるようになる

### [T3] 起動時にサンプル文書を自動で開く
- **ファイル**: `apps/desktop/src/renderer/store/useAppStore.ts` or `App.tsx`
- **内容**: 起動直後に `loadDocument(mockRootPath + '/welcome.md')` を自動実行
- **理由**: 空画面スタートだと訪問者がエディターを見るまでに操作が必要
- **工数**: 30分
- **完了条件**: ページを開いた瞬間にエディターと文章が表示されている
- **状態**: ⬜ 未着手

### [T4] モックのサンプルテキストをデモ向けに書き直す
- **ファイル**: `apps/desktop/src/preload/preloadMockData.ts`
- **内容**: 開発者向けダミーテキスト → 執筆シーンが伝わる内容に。3〜5段落・章あり・一部は解析済み状態
- **工数**: 30分
- **完了条件**: 読んで「小説・エッセイを書いているシーン」が伝わるテキストになっている
- **状態**: ⬜ 未着手

### [T5] Vite の base パスを GitHub Pages 用に設定する
- **ファイル**: `apps/desktop/vite.config.ts`
- **内容**: `base: '/liteLizard/'` を追加（サブパス配信対応）
- **工数**: 15分
- **完了条件**: `vite build` 後の `dist/` で asset パスが `/liteLizard/` 始まりになっている
- **状態**: ⬜ 未着手

### [T6] GitHub Actions 自動デプロイを設定する
- **ファイル**: `.github/workflows/deploy-pages.yml`（新規）
- **内容**: main push → `vite build` → gh-pages ブランチへ deploy
- **工数**: 30〜60分
- **完了条件**: main へのプッシュ後、GitHub Pages の URL でアプリが起動する
- **状態**: ⬜ 未着手

### [T7] GitHub Pages URL で実機確認・チェックリストを通す
- **工数**: 半日（バッファ込み）
- **完了条件**: 以下がすべて ✅ になること:
  - [ ] GitHub Pages の URL でアプリが起動する
  - [ ] 起動直後にエディターと文章が表示されている
  - [ ] 段落を編集できる
  - [ ] ミクロ↔マクロ切り替えが動く
  - [ ] 段落・章のDnDが動く
  - [ ] チャットパネルを開ける
  - [ ] 「生成」ボタンで解析結果が表示される
  - [ ] スマホで致命的な崩れがない

### [T8] note 記事用のスクリーンショット・GIF を収録する
- **内容**: DnD → 解析実行の一連操作を録画
- **完了条件**: note 記事に貼れる画像・GIF が手元にある
- **状態**: ⬜ 未着手

### [T9] note 記事を書いて公開する
- **内容**: 「執筆×AI解析ツールを開発中」トーンで。デモURL・スクリーンショット・「β版・フィードバック歓迎」を明記
- **完了条件**: note の記事 URL が存在する
- **状態**: ⬜ 未着手

---

## ⚠️ 懸念・リスク

> タスクにはならないが、無視できない問題・リスクをここに記録する。詳細は `DECISIONS.md` 参照。

- ファイル形式が `.md`（仕様は `.lzl`）→ MVP後に移行が必要
- APIキー管理が仕様§9と乖離 → MVPスコープ外、本番前に要対応

---

## 💡 アイデアボックス

> 今は着手しないが将来やりたいこと。

- 縦書きモード（仕様上は「将来実装」）
- 章サマリー表示（分析ペイン）
- `.lzl` フォーマット策定
- DnD並び替えのUndo対応

---

## ✅ 完了済みタスク

> 10件を超えたら古い順から `ARCHIVE.md` へ移動する。

- [x] エディター（ミクロ/マクロ視点・Ctrl+Scroll切り替え）
- [x] 段落・章のドラッグ&ドロップ
- [x] エクスプローラー・ファイル操作（作成・リネーム・削除）
- [x] チャットパネルの開閉（Cmd/Ctrl+Shift+A）
- [x] `runAnalysis()` のストア実装
- [x] `createMockPreloadApi()` の完全実装（解析モック込み）
- [x] MVP 公開計画ドキュメント作成（`docs/mvp_github_pages_readiness.md`）
- [x] PROJECTMEMORY/ タスク管理システム構築（TASKS / WORKSPACE / DECISIONS / ARCHIVE）
